// @ts-check
import { defineConfig } from 'astro/config';

// site 影响 SEO 与 RSS 中的文章链接，部署后请改成你的正式地址，
// 例如 https://你的用户名.pages.dev 或绑定的自定义域名。
export default defineConfig({
  site: 'https://example-blog.pages.dev',
});
